//A render process encapsulates the rendering of the Scene to the viewport or a texture (or several).
//this way we can chain several process like rendering the Scene and applying postfx, or rendering to several textures.

//WORK IN PROGRESS

function RenderProcess()
{

}

RenderProcess.prototype.execute = function()
{
}

