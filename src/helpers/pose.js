/* A pose contains the transform of a set of nodes and allows to interpolate between them */
function Pose(o)
{
	//array containing object like { id:, transform: }
	this.nodes = []; 
}

Pose.prototype.applyPose = function()
{

}

Pose.prototype.addNode = function(node, transform)
{

}